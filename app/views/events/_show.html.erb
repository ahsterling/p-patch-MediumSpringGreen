<div  class="panel panel-success" id="home">
  <div class="panel-heading">
    <h3 class="panel-title"><%= link_to "Events", "/events"  %></h3></div>

    <table class="table table-hover">
    <%   @events.each do |meeting| %>
      <tr>
        <td><%=  meeting.title %>
          <%=  meeting.date %>
          </td>
      </tr>
    <% end %>

    </table>

</div>
