<%= @event.date.strftime('%m/%d/%Y')%>
<%= @event.title %>
<% if @event.user_id != nil%>
<%= @event.user.name %>
<% end %>

<% if current_user_match %>

<%= button_to "Edit", "/events/#{@event.id}/edit", {class: 'btn btn-info'} %>

<% else %>
<div></div>
<% end %>
